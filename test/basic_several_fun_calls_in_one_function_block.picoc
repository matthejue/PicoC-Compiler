// in:5
// expected: 12
// dependencies: ../../Pico-OS/library/stdio/libstdio.reti_blocks
#include "../../Pico-OS/library/stdio/stdio.header"

int add_one(int val) {
  return val + 1;
}

int double_it(int val) {
  return val * 2;
}

void main() {
  int __picoc_input_0;
  scanf(" %d", &__picoc_input_0);
  int input_val = __picoc_input_0;

  if (input_val < 10) {
    int tmp = add_one(input_val);
    printf(" %d", double_it(tmp));
  } else {
    printf(" %d", 0);
  }
}
