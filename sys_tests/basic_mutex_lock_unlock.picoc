// in:
// expected:42 84

#include "./include/mutex.h"

int main() {
    struct mutex m;

    mutex_init(&m);

    mutex_lock(&m);
    print(42);
    mutex_unlock(&m);

    m.lock = 1;
    mutex_lock(&m);
    print(84);
    mutex_unlock(&m);

    return 0;
}
