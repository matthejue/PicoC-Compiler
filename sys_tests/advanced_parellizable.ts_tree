translation_unit: int compute(int x) {\n    return x * 2;  \n}\n\nint main() {\n    int i;\n    int sum;\n    int value; \n    i = 0;\n    sum = 0;\n\n    while (i < 8) {\n        value = i * 3 + i * 2;\n        sum = sum + compute(value);\n        i = i + 1;\n    }\n\n    sum = sum * sum;\n\n    return 0;\n}\n
  function_definition: int compute(int x) {\n    return x * 2;  \n}
    primitive_type: int
    function_declarator: compute(int x)
      identifier: compute
      parameter_list: (int x)
        (: (
        parameter_declaration: int x
          primitive_type: int
          identifier: x
        ): )
    compound_statement: {\n    return x * 2;  \n}
      {: {
      return_statement: return x * 2;
        return: return
        binary_expression: x * 2
          identifier: x
          *: *
          number_literal: 2
        ;: ;
      }: }
  function_definition: int main() {\n    int i;\n    int sum;\n    int value; \n    i = 0;\n    sum = 0;\n\n    while (i < 8) {\n        value = i * 3 + i * 2;\n        sum = sum + compute(value);\n        i = i + 1;\n    }\n\n    sum = sum * sum;\n\n    return 0;\n}
    primitive_type: int
    function_declarator: main()
      identifier: main
      parameter_list: ()
        (: (
        ): )
    compound_statement: {\n    int i;\n    int sum;\n    int value; \n    i = 0;\n    sum = 0;\n\n    while (i < 8) {\n        value = i * 3 + i * 2;\n        sum = sum + compute(value);\n        i = i + 1;\n    }\n\n    sum = sum * sum;\n\n    return 0;\n}
      {: {
      declaration: int i;
        primitive_type: int
        identifier: i
        ;: ;
      declaration: int sum;
        primitive_type: int
        identifier: sum
        ;: ;
      declaration: int value;
        primitive_type: int
        identifier: value
        ;: ;
      expression_statement: i = 0;
        assignment_expression: i = 0
          identifier: i
          =: =
          number_literal: 0
        ;: ;
      expression_statement: sum = 0;
        assignment_expression: sum = 0
          identifier: sum
          =: =
          number_literal: 0
        ;: ;
      while_statement: while (i < 8) {\n        value = i * 3 + i * 2;\n        sum = sum + compute(value);\n        i = i + 1;\n    }
        while: while
        parenthesized_expression: (i < 8)
          (: (
          binary_expression: i < 8
            identifier: i
            <: <
            number_literal: 8
          ): )
        compound_statement: {\n        value = i * 3 + i * 2;\n        sum = sum + compute(value);\n        i = i + 1;\n    }
          {: {
          expression_statement: value = i * 3 + i * 2;
            assignment_expression: value = i * 3 + i * 2
              identifier: value
              =: =
              binary_expression: i * 3 + i * 2
                binary_expression: i * 3
                  identifier: i
                  *: *
                  number_literal: 3
                +: +
                binary_expression: i * 2
                  identifier: i
                  *: *
                  number_literal: 2
            ;: ;
          expression_statement: sum = sum + compute(value);
            assignment_expression: sum = sum + compute(value)
              identifier: sum
              =: =
              binary_expression: sum + compute(value)
                identifier: sum
                +: +
                call_expression: compute(value)
                  identifier: compute
                  argument_list: (value)
                    (: (
                    identifier: value
                    ): )
            ;: ;
          expression_statement: i = i + 1;
            assignment_expression: i = i + 1
              identifier: i
              =: =
              binary_expression: i + 1
                identifier: i
                +: +
                number_literal: 1
            ;: ;
          }: }
      expression_statement: sum = sum * sum;
        assignment_expression: sum = sum * sum
          identifier: sum
          =: =
          binary_expression: sum * sum
            identifier: sum
            *: *
            identifier: sum
        ;: ;
      return_statement: return 0;
        return: return
        number_literal: 0
        ;: ;
      }: }