// in:41
// expected: 42
// dependencies: ../../Pico-OS/lib/stdio/libstdio.picoc
#include "../../Pico-OS/lib/stdio/stdio.header"

#define SIZE (3 * 2)

/* struct with array attribute */
struct Box {
    int data[SIZE];
};

/* function using last element of array */
int last_element(int arr[SIZE]) {
    int var = 2;
    return arr[SIZE - 1];
}

int main() {
    struct Box b;

    int __picoc_input_0;
    scanf(" %d", &__picoc_input_0);
    int x = __picoc_input_0;
    b.data[SIZE - 1] = x + 1;

    debug;
    printf(" %d", last_element(b.data));

    return 0;
}
