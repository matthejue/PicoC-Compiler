// in:42
// expected: 4 5 6 42 6 -1
// dependencies: ../../Pico-OS/lib/stdio/libstdio.reti_blocks
#include "../../Pico-OS/lib/stdio/stdio.header"

struct array_with_len {int ar[3]; int len;};

int print_all(struct array_with_len st_ar_copy);

void main() {
  // fun_call as statement
  struct array_with_len st_ar = {.ar={4, 5, 6}, .len=3};
  int success;
  success = print_all(st_ar);
  printf(" %d", st_ar.ar[2]);
  // fun_call as expression
  if (success) {
    printf(" %d", -1);
  }
}

int print_all(struct array_with_len st_ar_copy) {
  int i = 0;
  while (i < st_ar_copy.len) {
    printf(" %d", st_ar_copy.ar[i]);
    i = i + 1;
  }
  int __picoc_input_0;
  scanf(" %d", &__picoc_input_0);
  st_ar_copy.ar[2] = __picoc_input_0;
  printf(" %d", st_ar_copy.ar[2]);
  return 1;
}
