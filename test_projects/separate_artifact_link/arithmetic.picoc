#include "arithmetic.header"

int arithmetic_bias = 4;

int weighted_sum(struct NumberPair *values) {
    char *arithmetic_tag = "arith";
    return (*values).left * 2 + (*values).right * 3
        + arithmetic_bias + arithmetic_tag[0];
}
