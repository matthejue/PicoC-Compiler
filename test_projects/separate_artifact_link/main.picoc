#include "pipeline.header"

int main_adjustment = 2;

int main() {
    struct NumberPair values = {.left=3, .right=5};
    char *main_tag = "main";
    int result = project_value(&values) + main_adjustment + main_tag[0];
    return result == 354;
}
