* ( ) Todo
  - ( ) die ganzen unnötigen Name, Num entfernen, wo man einfach direkt die Zahl pasten könnte
  - ( ) die ganzen Stack und Atom über multiple line bei while und if else fixen
  - ( ) schauen, ob thread pool das ganze überhaupt schneller macht
  - ( ) NEG_RELS genauer betrachten
  - ( ) das placeholder Problem lösen
  - ( ) _create_block einfach nur string return lassen und diesen dann in Exp(GoTo) hüllen
  - ( ) Problem lösen, dass bei mehr als 2^10 Instructions das ganze nicht mehr funktioniert bei LOADI ACC rn.NAME(_next_instruction)
  - ( ) Für jede \{\} ein Namespace bei while, do while, for und if else
  - ( ) Unnötige Symboltabelleneinträge bei z.B. Funtkionen und Structs

  - ( ) Strings
  - ( ) void pointer
  - ( ) Funktionendatentyp

  - ( ) _single_line_comment das \[\] nach außen

  - ( ) funktionsaufruf prozedere schöner machen wie scholl wollte
  - ( ) überall zuerst lesen und dann stackpointer ändern
  - ( ) Token() das Token wegmachen
