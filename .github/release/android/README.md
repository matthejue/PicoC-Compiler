# PicoC Compiler for Android

This archive supports 64-bit ARM devices running Android 7 or newer in Termux.
Extract it in Termux and run `./picoc-compiler/picoc_compiler --help`.

The archive includes Python, the standard library, the Tree-sitter Python
binding, and both project grammars. It does not install Termux packages or use
the device's Python environment.
