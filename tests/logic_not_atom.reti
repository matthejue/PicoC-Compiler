# File logic_not_atom Start
# Main Funktion (void main ((char var) = Not((10 < (34 + 1))))) Start
LOADI SP 200;
# Zuweisung ((char var) = Not((10 < (34 + 1)))) Start
# Allokation (char var) Start
# Variable var vom Typ char mit Adresse 100 zur Symboltabelle hinzugefügt
# Allokation (char var) Ende
# Logische unäre Verknüpfung Not((10 < (34 + 1))) Start
# Logisches Atom (10 < (34 + 1)) Start
# Arithmetischer Operand 10 Start
SUBI SP 1;                             # Stack um eine Zelle erweitern
LOADI ACC 10;                          # Wert von 10 in ACC laden
STOREIN SP ACC 1;                      # Wert in oberste Stacke-Zelle
# Arithmetischer Operand 10 Ende
# Arithmetische binäre Operation (34 + 1) Start
# Arithmetischer Operand 34 Start
SUBI SP 1;                             # Stack um eine Zelle erweitern
LOADI ACC 34;                          # Wert von 34 in ACC laden
STOREIN SP ACC 1;                      # Wert in oberste Stacke-Zelle
# Arithmetischer Operand 34 Ende
# Arithmetischer Operand 1 Start
SUBI SP 1;                             # Stack um eine Zelle erweitern
LOADI ACC 1;                           # Wert von 1 in ACC laden
STOREIN SP ACC 1;                      # Wert in oberste Stacke-Zelle
# Arithmetischer Operand 1 Ende
LOADIN SP ACC 2;                       # Wert von 34 in ACC laden
LOADIN SP IN2 1;                       # Wert von 1 in IN2 laden
ADD ACC IN2;                           # Wert von (34 + 1) in ACC laden
STOREIN SP ACC 2;                      # Ergebnis in zweitoberste Stack-Zelle
ADDI SP 1;                             # Stack um eine Zelle verkürzen
# Arithmetische binäre Operation (34 + 1) Ende
LOADIN SP ACC 2;                       # Wert von 10 in ACC laden
LOADIN SP IN2 1;                       # Wert von (34 + 1) in IN2 laden
SUB ACC IN2;                           # 10 - (34 + 1) in ACC laden
JUMP< 3;                               # Ergebnis 1, wenn (10 < (34 + 1)) erfüllt
LOADI ACC 0;                           # Ergebnis 0, wenn (10 < (34 + 1)) nicht erfüllt
JUMP 2;
LOADI ACC 1;                           # Ergebnis 1, wenn 10 < (34 + 1) wahr
STOREIN SP ACC 2;                      # Ergebnis in zweitoberste Stack-Zelle
ADDI SP 1;                             # Stack um eine Zelle verkürzen
# Logisches Atom (10 < (34 + 1)) Ende
LOADI ACC 1;                           # 1 in ACC laden
LOADIN SP IN2 1;                       # Wert von (10 < (34 + 1)) in IN2 laden
OPLUS ACC IN2;                         # !((10 < (34 + 1))) in ACC laden
STOREIN SP ACC 1;                      # Ergebnis in oberste Stack-Zelle
# Logische unäre Verknüpfung Not((10 < (34 + 1))) Ende
LOADIN SP ACC 1;                       # Wert von Not((10 < (34 + 1))) in ACC laden
ADDI SP 1;                             # Stack um eine Zelle verkürzen
LOADI IN1 255;                         # Bitmaske 1 für char Datentyp erstellen
AND ACC IN1;                           # Wo in der Bitmaske eine 0 ist, werden die Bits ebenfalls zu 0
LOADI IN2 32768;                       # Bitvektor 10000000_00000000 laden
MULTI IN2 65536;                       # Bit 1 im Bitvektor um 16 Bits shiften: 10000000_00000000_00000000_00000000
ANDI ACC IN1;                          # Testen, ob Zahl negativ ist
JUMP== 3;                              # Signextension für negative Zahl überspringen, wenn Zahl positiv ist
LOADI IN1 -256;                        # Bitsmaske 2, die überall dort eine 1 hat, wo Bitmaske 1 eine 0 hat
OR ACC IN1;                            # Wo in der Bitmaske eine 1 ist, werden die Bits ebenfallls zu 1
STORE ACC 100;                         # Wert von Not((10 < (34 + 1))) in Variable var speichern
# Zuweisung ((char var) = Not((10 < (34 + 1)))) Ende
JUMP 0;
# Main Funktion (void main ((char var) = Not((10 < (34 + 1))))) Ende
# File logic_not_atom Ende
