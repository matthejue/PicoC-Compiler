// in:32
// expected:98 67 2 -1 1 0

void main() {
  shrink = 5
  symbol = -
  // ------------------------- Arithmetic expressions -------------------------
  int var = input();
  // shame precedence, mixing char and int
  print(var + 'C' - 1);
  // prec1 (*) > prec2 (-)
  print(-'C' + var * 'C');
  // ---------------------------- Logic expressions ---------------------------
  // atom in parenthesis > arith_exp
  print((var * 2 == 'A' - 1) * 2);
  // same precedence, not arithmetic and logic
  print(-(0 || !(var > 33 || var == 33)));
  // and and or
  print(0 || var * 2 + 1 >= 'A' && 12);
  // and > or, not on arith
  print((!var || var > 42) && 0);
}
