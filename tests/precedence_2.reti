# File ./tests/precedence_2.picoc Start
# Main Funktion (void main ((int var) = (8 + (4 - 2)))) Start
LOADI SP 200;
# Zuweisung ((int var) = (8 + (4 - 2))) Start
# Allokation (int var) Start
# Variable var vom Typ int mit Adresse 100 zur Symboltabelle hinzugefügt
# Allokation (int var) Ende
# Arithmetische binäre Operation (8 + (4 - 2)) Start
# Arithmetischer Operand 8 Start
SUBI SP 1;                             # Stack um eine Zelle erweitern
LOADI ACC 8;                           # Wert von 8 in ACC laden
STOREIN SP ACC 1;                      # Wert in oberste Stacke-Zelle
# Arithmetischer Operand 8 Ende
# Arithmetische binäre Operation (4 - 2) Start
# Arithmetischer Operand 4 Start
SUBI SP 1;                             # Stack um eine Zelle erweitern
LOADI ACC 4;                           # Wert von 4 in ACC laden
STOREIN SP ACC 1;                      # Wert in oberste Stacke-Zelle
# Arithmetischer Operand 4 Ende
# Arithmetischer Operand 2 Start
SUBI SP 1;                             # Stack um eine Zelle erweitern
LOADI ACC 2;                           # Wert von 2 in ACC laden
STOREIN SP ACC 1;                      # Wert in oberste Stacke-Zelle
# Arithmetischer Operand 2 Ende
LOADIN SP ACC 2;                       # Wert von 4 in ACC laden
LOADIN SP IN2 1;                       # Wert von 2 in IN2 laden
SUB ACC IN2;                           # Wert von (4 - 2) in ACC laden
STOREIN SP ACC 2;                      # Ergebnis in zweitoberste Stack-Zelle
ADDI SP 1;                             # Stack um eine Zelle verkürzen
# Arithmetische binäre Operation (4 - 2) Ende
LOADIN SP ACC 2;                       # Wert von 8 in ACC laden
LOADIN SP IN2 1;                       # Wert von (4 - 2) in IN2 laden
ADD ACC IN2;                           # Wert von (8 + (4 - 2)) in ACC laden
STOREIN SP ACC 2;                      # Ergebnis in zweitoberste Stack-Zelle
ADDI SP 1;                             # Stack um eine Zelle verkürzen
# Arithmetische binäre Operation (8 + (4 - 2)) Ende
LOADIN SP ACC 1;                       # Wert von (8 + (4 - 2)) in ACC laden
ADDI SP 1;                             # Stack um eine Zelle verkürzen
STORE ACC 100;                         # Wert von (8 + (4 - 2)) in Variable var speichern
# Zuweisung ((int var) = (8 + (4 - 2))) Ende
JUMP 0;
# Main Funktion (void main ((int var) = (8 + (4 - 2)))) Ende
# File ./tests/precedence_2.picoc Ende
